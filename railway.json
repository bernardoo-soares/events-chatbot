{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "DATABASE_PATH=${DATABASE_PATH:-data/demo_events.db} PYTHONPATH=/app/src:src python -m uvicorn event_chatbot.main:app --host 0.0.0.0 --port $PORT"
  }
}
