[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install -e ."
]

[start]
cmd = "PYTHONPATH=src python -m uvicorn event_chatbot.main:app --host 0.0.0.0 --port $PORT"
